package chapter8.task_17;

public class Starship {
    public static void main(String[] args) {
        AlertStatus alertStatus = new AlertStatus();
        alertStatus.status();
        alertStatus.switchStatus();
        alertStatus.status();
    }
}
